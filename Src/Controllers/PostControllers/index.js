const { post } = require("../../Models");

const getPosts = async (req, res, next) => {};

const getPostUsers = (req, res, next) => {};
const updatePosts = (req, res, next) => {};

const deletePosts = (req, res, next) => {};

module.exports = {
  getPosts,
  getPostUsers,
  updatePosts,
  deletePosts,
};
